<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Personalexpensemanagement"
        tools:targetApi="31">
        <activity
            android:name=".controller.activity.account.AddAccountActivity"
            android:exported="false" />
        <activity
            android:name=".controller.activity.account.AccountActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".controller.activity.transaction.activity.TransactionAddActivity_SelectionWallet"
            android:exported="false" />
        <activity
            android:name=".controller.activity.transaction.activity.TransactionAddActivity_SelectionAccount"
            android:exported="false" />
        <activity
            android:name=".controller.activity.transaction.activity.TransactionAddActivity"
            android:exported="false" />
        <activity
            android:name=".controller.activity.transaction.activity.TransactionFilterActivity"
            android:exported="false" />
        <activity
            android:name=".controller.activity.RootActivity"
            android:exported="false" />
        <activity
            android:name=".controller.activity.MainActivity"
            android:exported="false" />
        <activity
            android:name=".controller.activity.login.LoginActivity"
            android:exported="true">

        </activity>
    </application>

</manifest>